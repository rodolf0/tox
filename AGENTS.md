# Agent strict guidelines
- Never delete comments without user approval. If they are outdated suggest an update and wait for approval.
- Never commit to source control. You may only use read-only source control operations.
- Never delete assertions (even in tests) or redirect them to different code paths just to satisfy the compiler.
- Never optimize at the expense of API ergonomics and code readability.
